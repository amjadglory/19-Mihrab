import { DragDropImg } from './drag-drop-img';

describe('DragDropImg', () => {
  it('should create an instance', () => {
    const directive = new DragDropImg();
    expect(directive).toBeTruthy();
  });
});
